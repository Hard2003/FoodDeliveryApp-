# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/FoodDelivery

# OR for MongoDB Atlas (replace with your actual connection string)
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name

# Server Configuration
PORT=5000

# JWT Secret (generate a random string for production)
JWT_SECRET=your_super_secret_jwt_key_replace_this_with_random_string

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Development Settings
NODE_ENV=development
REQUIRE_RESTAURANT_APPROVAL=false
